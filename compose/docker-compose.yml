name: otp-secured-cloud

services:
  openldap:
    image: osixia/openldap:1.5.0
    container_name: otpsec-openldap
    hostname: openldap
    environment:
      LDAP_ORGANISATION: "${LDAP_ORGANISATION}"
      LDAP_DOMAIN: "${LDAP_DOMAIN}"
      LDAP_ADMIN_PASSWORD: "${LDAP_ADMIN_PASSWORD}"
      LDAP_CONFIG_PASSWORD: "${LDAP_CONFIG_PASSWORD}"
      LDAP_RFC2307BIS_SCHEMA: "true"
      LDAP_REMOVE_CONFIG_AFTER_SETUP: "false"
      LDAP_TLS: "false"
    command: ["--copy-service"]
    ports:
      # LDAPS (636) aún no se mapea: queda disponible solo dentro de la red
      # Docker. Se publicará en el host cuando se active TLS en una fase
      # posterior, usando un puerto alterno (p.ej. 6636) para evitar
      # colisiones con otros servicios LDAPS del equipo.
      - "389:389"
    volumes:
      - ../ldap/bootstrap:/container/service/slapd/assets/config/bootstrap/ldif/custom:ro
      - ldap_data:/var/lib/ldap
      - ldap_config:/etc/ldap/slapd.d
    networks:
      - otpsec
    restart: unless-stopped

volumes:
  ldap_data:
  ldap_config:

networks:
  otpsec:
    driver: bridge
